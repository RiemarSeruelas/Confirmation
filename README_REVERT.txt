This ZIP restores the project to the version before the YouTube/mobile navigation update.

Use:
1. Extract this ZIP.
2. Replace your project files with these files.
3. Run npm install if node_modules is missing.
4. Run npm run dev.

Important structure:
- index.html is at the project root.
- React files are inside src/.
- index.html points to /src/main.jsx.
